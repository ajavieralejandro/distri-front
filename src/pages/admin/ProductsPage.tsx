import { PlaceholderPage } from '@/shared/components/PlaceholderPage';

export function ProductsPage() {
  return (
    <PlaceholderPage
      title="Productos"
      description="Esta funcionalidad se implementará en la etapa de catálogo."
    />
  );
}
