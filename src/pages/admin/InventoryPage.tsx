import { PlaceholderPage } from '@/shared/components/PlaceholderPage';

export function InventoryPage() {
  return (
    <PlaceholderPage
      title="Inventario"
      description="La consulta y ajuste de stock por depósito se implementará en la etapa de inventario."
    />
  );
}
