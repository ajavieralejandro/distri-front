import { PlaceholderPage } from '@/shared/components/PlaceholderPage';

export function AccountPage() {
  return (
    <PlaceholderPage
      title="Cuenta corriente"
      description="La consulta de saldos y comprobantes se implementará en la etapa de cuenta corriente."
    />
  );
}
