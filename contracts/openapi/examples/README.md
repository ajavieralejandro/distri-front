# Examples

Request/response examples live inline on each OpenAPI operation under `paths/*.yaml`
(and survive into `dist/distrisoft-api.bundled.yaml`).

Additional shared examples can be added here as `$ref` targets when duplication grows.
